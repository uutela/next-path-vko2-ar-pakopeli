{
  "name": "coaching_session_schema",
  "description": "A coach question and the structured answer that was returned.",
  "fields": [
    {"name": "session_id", "type": "string", "required": true, "description": "Unique session identifier."},
    {"name": "query_text", "type": "string", "required": true, "description": "Family question."},
    {"name": "intent", "type": "string", "required": true, "description": "plan, contributions, or both."},
    {"name": "locale", "type": "string", "required": false, "description": "en or fi."},
    {"name": "answer", "type": "string", "required": true, "description": "Prose answer."},
    {"name": "created_at", "type": "string", "required": true, "description": "ISO timestamp."}
  ]
}
