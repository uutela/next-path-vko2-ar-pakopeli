{
  "name": "household_snapshot_schema",
  "description": "Latest household document posted from the family app.",
  "fields": [
    {"name": "snapshot_id", "type": "string", "required": true, "description": "Unique snapshot identifier."},
    {"name": "household", "type": "object", "required": true, "description": "Full Household JSON document."},
    {"name": "source", "type": "string", "required": false, "description": "cli, api, or ui."},
    {"name": "created_at", "type": "string", "required": true, "description": "ISO timestamp."}
  ]
}
